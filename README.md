# What Do I Need?（WDIN）v1.1.7

基于 NeoForge 21.1（Minecraft 1.21.1）的纯客户端材料追踪模组，**硬依赖 JEI**。
核心功能对标 "JEI 配方标记 + 缺料清单显示"。

## 功能

- 在 JEI 配方界面 / 背包 / 容器界面按下 **Tab**，把光标下的配方加入追踪列表（新追踪的配方会自动选中）；JEI 同屏展示多个配方时，按鼠标停留的**具体配方**精确追踪
- HUD 配方行显示对应工作方块：`物品名 于 工作方块 制造`（"于"与"制造"为灰色，工作方块图标+名字为正常字号）
- 材料匹配**智能识别 JEI 配方声明的接受物品/标签**：配方是具体物品就只认该物品；配方接受某个标签（如"任意木板"）才按该标签合并计数，配方限定之外的物品不会被误判
- 可配置是否忽略 NBT 差异（默认忽略）
- 在 JEI 物品列表或背包悬停物品按 **N**，追踪该物品本身 / 取消追踪
- 未打开任何界面（或打开背包/容器但光标下没有物品）时，**单击 Tab** 顺序切换选中的追踪配方；**按住 Tab + 鼠标滚轮** 上下切换
- **↑ / ↓** 直接选择上一个 / 下一个追踪配方（可自定义）
- 按 **V** 移除当前选中的追踪配方
- 按 **H**（可自定义）打开选中配方的数量调整界面：**滑动条（1–256）** 快速设置数量，下方保留 **-10 / -1 / +1 / +10** 按钮（按钮调整同样限制在 1–256 内）与移除/关闭
- HUD 的鼠标交互（右键选中、左键拖拽、双击配置菜单、悬停滚轮调大小）仅在**游戏内**或**聊天框打开**时可用；暂停界面、JEI、背包/容器等其余一切界面均禁用，不会干扰正常操作
- HUD 上**双击**打开 HUD 配置菜单：透明度、大小、单列配方数（1–64）
- 单列配方数满后自动换列，新列显示在右侧，第一列位置不变（默认 10）
- 自动扫描主背包、快捷栏、副手以及打开的箱子/木桶等容器内的物品（每 200ms 节流）
- 容器中所需材料会被高亮（绿色=足够，红色=不足）
- 未追踪任何配方时 HUD 完全隐藏
## 按键

| 按键 | 功能 | 可改 |
| --- | --- | --- |
| Tab | 无物品选中时单击切换选中配方；界面内光标下有物品时追踪该物品 | 是 |
| N | 追踪该物品本身 / 取消追踪（光标下的物品） | 是 |
| V | 移除当前选中的追踪配方 | 是 |
| 鼠标中键 | 打开选中配方的数量调整界面 | 是 |
| ↑ / ↓ | 选择上一个 / 下一个追踪配方 | 是 |
| 双击 | 在 HUD 上双击打开 HUD 配置菜单（透明度/大小/单列配方数） | - |

按键可在 设置 -> 控制 -> What Do I Need? 中修改。

## 配置

- `config/wdin-client.toml`：HUD 样式、扫描规则、颜色等（NeoForge 标准配置）
- `.minecraft/wdin/hud.json`：HUD 位置/大小/透明度/单列配方数（拖拽或菜单调整后自动保存）
- `.minecraft/wdin/tracked.json`：追踪列表数据

## 安装

1. 需要 Minecraft **1.21.1** + NeoForge **21.1.238 及以上** + JEI **19.21.0.247 及以上**（强制依赖 JEI，缺失会无法启动；兼容 19.21.0.247 往上的主流版本，包括 ATM10 的 19.39.x）
2. 将 `wdin-1.1.7.jar` 放入 `.minecraft/mods/`
3. 启动游戏

## 更新日志

- **1.1.7**：修复材料匹配错位问题——每种材料按配方自身的 Ingredient 声明（与 JEI 转移物品同一套识别数据）匹配，不再出现"木炭/木棍被原木顶替"或"背包物品不被识别"；自查加固：聊天框/铁砧改名/告示牌等文字输入界面不再被快捷键劫持（打字正常）、主菜单等无玩家场景按键防护、同一配方不同显示代表不再重复添加、纯标签材料匹配兜底
- **1.1.6**：HUD 鼠标交互收紧为仅游戏内/聊天框打开时可用（暂停界面、JEI、背包/容器等一律禁用拖拽与双击等）
- **1.1.5**：打开背包/容器时禁用 HUD 鼠标交互（拖拽、双击、右键、滚轮调大小），点击可正常穿透操作背包，避免多列布局时的干扰；聊天框等非容器界面仍可交互
- **1.1.4**：修复追踪较多配方/换列后 HUD 不显示、出现透明空块的问题（移除过时的行数截断，所有配方按列完整渲染）
- **1.1.3**：修复多配方时后添加条目显示为空/占位的问题（渲染异常隔离，单块出错不再影响其他行）；工作方块显示改为"于 工作方块 制造"且恢复正常字号
- **1.1.2**：新增 ↑/↓ 键选择上/下一个配方（可自定义，原切换键保留）；HUD 配方行显示"物品名 于 工作方块"（灰色"于"+工作方块图标）；默认键调整：切换配方 Tab、数量界面鼠标中键（N/V 不变）
- **1.1.1**：数量调整界面新增 1–256 滑动条，±按钮移到滑动条下方且不超 256；JEI 兼容范围放宽至 19.21.0.247 及以上（公开 API 全部兼容，内部配方定位做了 19.21/19.39/19.4x 三套适配，个别旧版拿不到配方布局时自动降级为悬停物品反查，其余功能不受影响）
- **1.1.0**：移除 ←/→ 切换合成方案功能（存在不稳定问题）；保留并沿用 JEI 配方界面按鼠标停留位置精确追踪
- **1.0.9**：新增 ←/→ 切换选中配方的其他合成方案；修复 JEI 19.39（ATM10）下多配方界面按 G 追踪不准的问题（现在按鼠标停留的具体配方精确追踪）
- **1.0.8**：模组更名为 **What Do I Need?（WDIN）**（modid、包名、语言键、存档路径等全部同步）；材料匹配升级为"智能识别 JEI 配方声明的接受物品/标签"（具体物品只认该物品，声明了标签的才按标签匹配），修复 1.0.7 过于严格 / 旧版过宽的问题
- **1.0.7**：材料匹配改为严格按 JEI 配方物品（Tag 合并关闭），修复 ATM10 中几乎所有物品被误识别/误高亮的问题
- **1.0.6**：兼容 JEI 19.39.x（ATM10 整合包自带版本），依赖范围放宽为 19.39.0.368 及以上
- **1.0.5**：HUD 自动换列（双击配置菜单新增"单列配方数"1–64）
- **1.0.4**：数量调整界面居中重排；N 键描述更新；双击 HUD 配置菜单；悬停滚轮调大小；G 单击切换、长按 G+滚轮上下切换
- **1.0.3**：HUD 配方选择功能（V 移除选中、H 数量界面、右键选中）
- **1.0.1**：未追踪配方时 HUD 完全隐藏
- **1.0.0**：初始版本（RCPTracker）

## 重新构建

双击 `build_mod.bat` 即可（自动使用
`C:\Users\MaskedFish\AppData\Roaming\.minecraft\runtime\java-runtime-delta` 下的 JDK21）。

注意：本机网络环境下 Java 直连 `maven.neoforged.net` 等部分主机可能被重置，因此构建脚本会先启动本地转发代理（`build-tools/neoforge_proxy.ps1` + `RcpProxy.exe`），再由 Gradle 通过代理拉取依赖。
